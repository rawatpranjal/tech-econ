baseURL = 'https://tech-econ.com/'
languageCode = 'en-us'
title = 'tech-econ'

[params]
  description = "Real tools for applied researchers. Packages, datasets, and learning resources."
  author = "Pranjal Rawat"
  formUrl = ""  # Add your Google Form URL here

  # Analytics Tracker Configuration
  # Set trackerEndpoint to your Cloudflare Worker URL to enable tracking
  # trackerEndpoint = "https://your-analytics.workers.dev/events"
  trackerEnabled = true
  trackerDebug = false

[build]
  writeStats = true

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

# RSS Feed
[outputs]
  home = ["HTML", "RSS"]
  section = ["HTML", "RSS"]

[outputFormats.RSS]
  mediaType = "application/rss+xml"
  baseName = "feed"
