baseURL = 'https://rawatpranjal.github.io/tech-econ/'
languageCode = 'en-us'
title = 'tech-econ'

[params]
  description = "A searchable directory of Python packages for econometrics, causal inference, and time series analysis"
  author = "Pranjal Rawat"
  formUrl = ""  # Add your Google Form URL here

[build]
  writeStats = true

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

# RSS Feed
[outputs]
  home = ["HTML", "RSS"]
  section = ["HTML", "RSS"]

[outputFormats.RSS]
  mediaType = "application/rss+xml"
  baseName = "feed"
