# Dev/test dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Production deps live in requirements.txt. Anything imported only by tests/
# or tooling belongs here.

pytest>=8.0
pytest-cov>=5.0
ruff>=0.5
mypy>=1.10

# Lightweight numerical libs needed by lib/ unit tests. NOT a full
# replacement for requirements.txt — heavy ML deps (sentence-transformers,
# lightgbm) stay there.
numpy>=1.26

# Clustering scripts (cluster_resources, cluster_hdbscan, cluster_topics) are
# tested at the pure-function level and import these at module load time.
# Both have pre-built wheels — install is fast in CI.
scikit-learn>=1.3
hdbscan>=0.8
