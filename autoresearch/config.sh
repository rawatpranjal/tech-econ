#!/usr/bin/env bash
# autoresearch/config.sh - Shared configuration defaults

export AR_MODEL="${AR_MODEL:-sonnet}"
export AR_MAX_ITERATIONS="${AR_MAX_ITERATIONS:-10}"
export AR_BUDGET_TOTAL="${AR_BUDGET_TOTAL:-20}"
export AR_BUDGET_PER_ITER="${AR_BUDGET_PER_ITER:-2}"
export AR_TIMEOUT="${AR_TIMEOUT:-1800}"
