# Changelog

## 2026-05-25 (loop iteration 44)
- **Test expansion — explore.js pure helpers, test_rank_content.py (18 tests)**: (a) New `tests/js/explore.test.js` (34 tests) — exercises `window.Explore._*` test surface: `_slugify` (empty, hyphen collapsing, leading/trailing strip), `_truncate` (null/undefined, exact-len, ellipsis), `_isPopularItem` (threshold boundary at 0.7), `_isNewItem` (fake timers, 5-day window), `_getDailyHeroSeed` (deterministic same-day, changes on new day), `_selectDailyHero` (null→null, deterministic same-day, varies by carouselId), `_getEngagementBadge` (New > Popular priority, old item→empty). (b) New `tests/python/scripts/test_rank_content.py` (18 tests) — `wilson_lower_bound` (zero n, upper bound ≤ phat, unit interval, tighter with more obs), `normalize` (empty, all-equal→0.5, min/max), `log_scale` (0→0, 100≈1.0, monotone). Full Python coverage of lib/ modules complete. Suite: **714 JS (+34), 1983 Python (+29)**.

## 2026-05-25 (loop iteration 43)
- **Test expansion — recategorize_talks, categorize_industry_blogs, assign_to_clusters**: (a) New `test_recategorize_talks.py` (23 tests) — `get_new_subtopic` across Causal/Platforms/AI/Industry/Labor macro-category branches, priority ordering (ML beats Experimentation keyword), Podcast Series type, unknown macro fallback. (b) New `test_categorize_industry_blogs.py` (15 tests) — `get_sector` name-mapping priority over description, AdTech boundary tokens, description-keyword fallback paths (rideshare, banking, aggregation theory, final fallback). (c) New `test_assign_to_clusters.py` (10 tests) — `build_clusters_from_assignments`: empty→empty, min_cluster_size filter, secondary dedup, model_score sort, max_carousel cap, item_count vs carousel_items, unknown cluster_id skipped, `primary="error"/"none"` ignored, `secondary="null"` not added. Suite: **680 JS (unchanged), 1954 Python (+48)**.

## 2026-05-25 (loop iteration 42)
- **Test expansion — sync_resources, reorganize_resources, generate_embeddings, fetch_og_images, build_engagement_scores (rank_all_content)**: (a) New `test_sync_resources.py` (23 tests) — `normalize_url` (prefix priority, trailing slash, lowercase) and `extract_urls_from_source` (resources/packages split, DOMAIN_MAPPING lookup). (b) New `test_reorganize_resources.py` (31 tests) — `transform_resource`: category consolidation, type normalization + title-case fallback, macro_category, domain backfill, difficulty normalization, in-place mutation. (c) New `test_generate_embeddings.py` (20 tests) — `slugify` and `combine_text_for_embedding` (embedding_text path vs legacy fallback, field priority). (d) New `test_fetch_og_images.py` (11 tests) — `extract_youtube_thumbnail` (watch, youtu.be, embed URLs; channel URLs→None). (e) Added `TestBuildEngagementScores` (10 tests) to `test_rank_all_content.py` — caught non-obvious interaction: items with ≥10 impressions and 0 clicks receive a penalty that offsets the impression score. Suite: **680 JS (unchanged), 1906 Python (+95 this iteration)**.

## 2026-05-25 (loop iteration 41)
- **Test expansion — `_default_fetcher` (d1_client), recsys output invariants, `extract_repo_info`, `extract_item_name_from_path`/`safe_join` (rank_all_content), `slugify`/`build_prompt`/`_decode_image` (generate_hero_images)**: (a) `TestDefaultFetcher` (4 tests) in `test_d1_client.py`. (b) New `test_test_recsys_outputs.py` (20 tests) covering all 6 invariant-check functions with monkeypatched fixture dirs. (c) New `test_update_stars.py` (10 tests) for `extract_repo_info` — `.git` stripped via regex not rstrip. (d) New `test_generate_hero_images.py` (19 tests) — slugify/build_prompt/_decode_image. (e) New `test_rank_all_content.py` (16 tests) — `extract_item_name_from_path` and `safe_join`. Suite: **680 JS (unchanged), 1811 Python (+65 this iteration)**.

## 2026-05-25 (loop iteration 40)
- **Test expansion — Wrangler 4.x preamble stripping, `_find_latest_experiment_report`**: (a) Added `TestFetchEventsViaWrangler` (9 tests) to `tests/python/lib/test_d1_sessions.py` — the new Wrangler 4.x preamble stripping logic (`re.search(r'\[.*\]', stdout, re.DOTALL)`) was completely untested. Covers: happy path returns rows, preamble stripped and rows extracted, nonzero exit→SessionLoadError, no `[...]` bracket→"no JSON array" error, invalid JSON inside brackets→"JSON parse failed" error, FileNotFoundError→"invocation failed", SubprocessError→"invocation failed", unknown payload shape→empty list, empty results→empty list. (b) Added `TestWranglerQuery` (6 tests) to `tests/python/scripts/test_analyze_experiments.py` — same preamble stripping also exists in `analyze_experiments._wrangler_query`: clean JSON, Wrangler 4.x preamble, no bracket→ValueError, invalid JSON→ValueError, empty results→[], empty payload→[]. (c) Added `TestFindLatestExperimentReport` (6 tests) to `tests/python/scripts/test_build_site_scoreboard.py` — `_find_latest_experiment_report` was fully untested: missing dir→None, no matching files→None, single file returned, lexicographically latest date wins, different experiment_id ignored, non-date-pattern files ignored. Suite: **680 JS (unchanged), 1742 Python (+21)**.

## 2026-05-25 (loop iteration 39)
- **Bug fix + 8 new tests — test isolation (`test_migrate_data_helpers.py` lib poison) + `check_url` error paths**: (a) Fixed cross-test contamination in `tests/python/scripts/test_migrate_data_helpers.py`: it registered `sys.modules["lib"]` as a plain `ModuleType` stub which poisoned the real `lib` package for later tests, causing 19 failures in the full suite (`test_evaluate_recsys.py` + `test_generate_homepage_rows.py`) with `'lib' is not a package`. Fix: removed the stub — `migrate_data.py` only imports `lib.data_io` lazily inside `apply()`, not at module level, so no stub is needed. (b) Added 8 new `TestCheckUrlNetworkPaths` tests to `test_validate_data.py` — HEAD 200→no error, HEAD 400→GET 200 fallback, HEAD 400+GET 404→HTTP error message, Timeout→"Timeout", SSL error→"SSL" in message, Connection error→"Connection" in message, generic exception→"Error" in message, returned URL matches input. Used inline `FakeException` subclasses + `patch("scripts.validate_data.requests.exceptions", ...)` to stay isolation-safe across the full suite (avoids dependency on potentially-mocked `requests.exceptions.*`). Suite: **680 JS (unchanged), 1721 Python (+8)**.

## 2026-05-25 (loop iteration 38)
- **Test expansion — unified-search `escapeHtml`/`highlightText` + source exposure**: Added `escapeHtml` and `highlightText` to `window.UnifiedSearch._helpers` in `static/js/search/unified-search.js` (they were internal only). Added 9 new tests to `tests/js/unified-search-helpers.test.js`: `escapeHtml` (script tag injection, falsy input→empty, plain text passthrough, ampersand), `highlightText` (mark wraps match, no-query escapes text, falsy input, case-insensitive, exact match preserved). Integration scan complete: all 16 lib/ modules tested, all autoresearch/checks tested, all worker helpers tested, all JS pure functions reachable via exposed API. Suite: **680 JS (+9), 1713 Python (unchanged)**.

## 2026-05-25 (loop iteration 37)
- **Test expansion + bug fix — check_homepage_visual CI validator, fetch_logo_fallbacks `get_root_domain`**: (a) Fixed bug in `autoresearch/checks/check_homepage_visual.py` line 144: `baseline_path.write_text()` crashed with `FileNotFoundError` because parent `autoresearch/` directory wasn't created in fresh tmp environments — added `baseline_path.parent.mkdir(parents=True, exist_ok=True)`. (b) New `test_check_homepage_visual.py` (13 tests) — CSS missing→1, CSS too small→1, brace mismatch→1, valid CSS passes→0, template missing→1, marker missing→1, Hugo failure→1, Hugo exception→1, CSS regression guard fires when lines drop >500→1, small change within threshold→0, no baseline creates one. (c) New `test_fetch_logo_fallbacks_helpers.py` (12 tests) for `get_root_domain` — simple domain, www stripped, subdomain stripped, blog subdomain, co.uk preserved (3-part), .org, .edu, .net, None→None, lowercase output, path+query ignored, www+subdomain combination. Suite: **671 JS (unchanged), 1713 Python (+25)**.

## 2026-05-25 (loop iteration 36)
- **Test expansion — check_deep_metadata CI validator, dashboard timeseries stat cards**: (a) New `test_check_deep_metadata.py` (12 tests) for `autoresearch/checks/check_deep_metadata.py` — valid→exit 0, missing packages.json→1, packages not a list→1, missing schema_version→1, invalid math_level→1, invalid confidence→1, invalid relationship type→1, valid relationship type passes, method missing name→1, key_concept missing description→1, regression guard fires when enriched drops, packages without deep_metadata silently skipped→0. (b) Added 3 `renderTraffic` tests to `tests/js/dashboard.test.js` covering the new timeseries-based stat cards (dashboard.js PR #56): last-entry pageviews/sessions used, missing fields render `0` not `--`, empty timeseries shows `--`. Suite: **671 JS (+3), 1688 Python (+12)**.

## 2026-05-25 (loop iteration 35)
- **Test expansion — generate_hero_images `_decode_image`, generate_homepage_rows `load_content_lookup`/`load_json`, check_homepage_rows CI validator**: (a) Added 6 `TestDecodeImage` tests to `test_generate_hero_images_helpers.py` — b64_json path returns correct bytes, empty payload→None, empty data list→None, no b64/url→None, null data→None, PNG magic bytes roundtrip. (b) Added `TestLoadJson` (4 tests) + `TestLoadContentLookup` (7 tests) to `test_generate_homepage_rows.py` — GitHub avatar URL construction (`github.com/{owner}.png?size=128`), OpenLibrary ISBN image URL, `papers_flat.json` uses title-as-name, URL priority chain (url>github_url>docs_url), items-without-name skipped, key lowercased+stripped, missing file graceful. (c) New `test_check_homepage_rows.py` (9 tests) for `autoresearch/checks/check_homepage_rows.py` — valid→0, missing rows key→1, file missing→1, invalid JSON→1, too-few rows→1, missing required row field→1, too-many duplicates→1, missing expected types→1, items_mmr optional→0. Suite: **668 JS (unchanged), 1676 Python (+26)**.

## 2026-05-25 (loop iteration 34)
- **Integration guard — exp_re1_mmr_v1 draft state + JS MMR topK=0**: (a) Added assertion to `aa-experiment-config.test.js` that `exp_re1_mmr_v1` does NOT appear in `getAllAssignments()` — guards against accidental early promotion before harness_aa_v2 clears. (b) Added `topK=0 returns empty array` to `mmr.test.js` to align with Python `test_top_k_zero_returns_empty`. Full audit complete: all lib/ modules covered, all scripts either have helpers tests or are thin CLI wrappers, all untested JS files (auth-ui, favorites-ui, theme, toggle, supabase-client) are pure DOM orchestration with no extractable logic. Suite: **668 JS (+1), 1650 Python (unchanged)**.

## 2026-05-25 (loop iteration 33)
- **Test gap closed — diversity `id_field` parameter**: `generate_homepage_rows.py` calls `mmr_rerank(..., id_field="name")` but `test_diversity.py` only tested the default `id_field="id"`. Added 2 regression tests: `test_custom_id_field` (items keyed by "name", lookup receives name strings, correct reordering) and `test_items_missing_custom_id_field_appended_at_end` (item without the custom key has no embedding → appended last, not skipped silently). Suite: **667 JS (unchanged), 1650 Python (+2)**.

## 2026-05-25 (loop iteration 31–32)
- **Test expansion — llm-worker helpers, tracker remaining helpers, regression tests**: (a) New `tests/js/llm-worker-helpers.test.js` (19 tests) — first test file for `llm-worker/index.js`; covers `isAllowedOrigin` (tech-econ.com, www, rawatpranjal.github.io, localhost:1313, rejects evil/null/empty/spoof), `corsHeaders` (reflects allowed origin, falls back to ALLOWED_ORIGINS[0] for unknown — not `*`), `jsonResponse` (200 default, custom status, Content-Type header, JSON body), `sseError` (Response, text/event-stream, SSE data format with error key). (b) Expanded `tracker-helpers.test.js` from 11→36 tests (+25): added `_jaccard` (identical=1, different=0, partial 1/3 exact value, case-insensitive), `_hash` (string return, deterministic, base-36 no non-[0-9a-z] beyond leading -), `_truncate` (null/undefined→empty, unchanged, ellipsis, coerces number), `_getReferrerSource` (direct/google/twitter/x.com-as-twitter/hackernews/github/linkedin/internal/referral) using `Object.defineProperty` configurable getter pattern. (c) Python regression tests: `test_update_stars_helpers.py` +3 (rstrip charset bug: "econometrist"/"causal-dag" not stripped, ".git" suffix stripped correctly), `test_split_book.py` +2 (re.MULTILINE bug: chapters 2+ detected in multi-line text), `test_validate_data.py` +1 (papers_flat category-not-topic duplicate detection). Also ran full audit of all JS/Python modules — no new coverage gaps found. Suite: **667 JS (+49 from iter 30), 1648 Python (+6)**.

## 2026-05-25 (loop iteration 29–30)
- **Test expansion — d1_sessions private helpers, narrative carousel helpers, discover_content digest, postprocess helpers, enrich_metadata_v2 apply_enrichment**: (a) Added `_normalise_name` (7 tests) + `_parse_event_data` (10 tests) directly to `test_d1_sessions.py` — covers dict passthrough, JSON parsing, empty/whitespace/non-string→None, JSON list/scalar→None, nested dict ok. (b) Added `build_item_lookup` (9 tests) + `merge_carousels` (6 tests) to `test_generate_narrative_carousels_helpers.py` — exposed real bug: empty `name=""` uses `""` not title fallback (documented in test). (c) Added `generate_digest` (10 tests) to `test_discover_content_helpers.py` — pure HTML report generator. (d) Extended `test_postprocess_clusters_helpers.py`: `is_career_cluster` (6), `get_career_industry` (5), `is_generic_label` (5) — caught "fintech" triggers "tech" first due to dict insertion order (documented). (e) Added `apply_enrichment` (8 tests) to `test_enrich_metadata_v2_helpers.py` — base fields, extended type-specific fields, empty extended not applied, unknown type falls back to base only. Suite: **618 JS (unchanged), 1642 Python (+66 from session start)**.

## 2026-05-25 (loop iteration 25–28)
- **Test expansion — multi-module coverage push**: (a) Added `expandQuery` + `setSynonyms` to search-worker test surface (6 new JS tests). (b) Added `merge_carousels` class to `test_generate_narrative_carousels.py` (7 tests). (c) New `test_categorize_bloggers_helpers.py` (14 tests) — `categorize_blogger` keyword classifier. (d) New `test_categorize_industry_blogs_helpers.py` (15 tests) — `get_sector` classifier. (e) Extended `test_rank_all_content_pure.py` with `apply_citations_boost` (9 tests). (f) Added `corsHeaders` + `jsonResponse` to analytics-worker test surface (8 new JS tests). (g) Extended `test_generate_narrative_carousels.py` with `generate_person_carousels` (7 tests) and `generate_journey_carousels` (8 tests). (h) Added `exportJSON` (3 tests) and `exportCSV` (4 tests) to `favorites-local.test.js` — covers Blob content capture, JSON serialization, CSV header/quote-escaping/field mapping; fixed `vi.fn()` can't be a constructor by using `window.Blob = class BlobCapture { ... }` pattern. Suite: **605 JS (+21), 1562 Python (+60)**.

## 2026-05-25 (loop iteration 24)
- **Test expansion — search-worker.js pure helpers + dashboard renderVariantStats**: (a) New `tests/js/search-worker-pure.test.js` (37 tests) — `escapeRegex` (dot/asterisk/paren/clean string/empty), `scoreSyntheticQuestions` (null inputs, full-query match, no-overlap, non-string entries, non-array guard), `getModelScoreBoost` (missing/NaN/0/1.0/0.5/out-of-range clamping), `getAudienceBoost` (missing audience/query, beginner↑↗ PhD↘, advanced↑, neutral, array audience join), `boostExactMatches` (exact→3x, starts-with→2x, no-match→1x, sort order, package boost, immutability), `reciprocalRankFusion` (empty inputs, keyword-only, merge, shared-item higher score, sort, rrfScore field, source=hybrid). Strategy: strip `importScripts()` calls, stub `window.MiniSearch`, append test surface. (b) Added 7 `renderVariantStats` tests to `tests/js/dashboard.test.js` — collecting message, per-variant blocks, CTR percentage formatting, missing CTR→`--`, XSS escape in variant id, impression/click counts. Suite: **584 JS (+44), 1502 Python (unchanged)**.

## 2026-05-25 (loop iteration 23)
- **Test expansion — recsys output invariants, normalize_scores, analytics-dashboard escapeHtml, test_recsys_outputs_checks.py**: Added `test_recsys_outputs_checks.py` (22 tests) for `check_model_scores_in_range`, `check_no_duplicate_ids_in_metadata`, `check_related_items_envelope`, `check_content_files_sorted_by_score`, `check_related_items_no_self_reference`, `check_related_items_ids_resolve` — all use `monkeypatch` to redirect `DATA_DIR`/`EMBEDDINGS_DIR` to `tmp_path`. Added 7 `TestNormalizeScores` tests to `test_rank_all_content_pure.py` (empty→empty, all-equal→0.5, min-max range, middle proportional, negative scores, single item). Exposed `_escapeHtml` on `window.AnalyticsDashboard` in `analytics-dashboard.js` and added 5 XSS-safety tests (`<script>` escaping, `&`, double-quotes safe in text nodes, empty string, plain text). Suite: **540 JS (+5), 1502 Python (+29 from iter 22)**.

## 2026-05-25 (loop iteration 22)
- **Test expansion — add_geocodes, validate_data check_url, assign_packages_to_clusters, rank_all_content pure, fetch_logo, fetch_og_images, homepage rows `make_item`, explore.js `scoreCluster`/`curatedSort`, sync_resources extract_urls, evaluate_recsys _scores_from_global_rankings**: New files: `test_add_geocodes_helpers.py` (12), `test_rank_all_content_pure.py` (26), `test_fetch_logo_helpers.py` (10), `test_fetch_og_images_helpers.py` (12), `test_assign_packages_to_clusters_helpers.py` (12). Extended: `test_validate_data.py` (+7 check_url skip-domain tests), `test_generate_homepage_rows.py` (+10 make_item), `explore-helpers.test.js` (+10 scoreCluster/curatedSort), `test_sync_resources_helpers.py` (+8 extract_urls), `test_evaluate_recsys.py` (+8 _scores_from_global_rankings). Also fixed null-guard bug in `explore.js:getClusterItems` (crash when `clusterData=null`). Suite: **535 JS, 1431 Python (+125 from iter 20 baseline)**.

## 2026-05-25 (loop iteration 20)
- **Test expansion — download images, enrich_batch prompt, assign_to_clusters, submit-worker schema**: (a) `test_download_image_helpers.py` (25 tests) — `get_domain` (conference images: path strip, www-preservation, no-scheme), `slugify` (dataset images), `get_extension` (webp/png/content-type/default), `get_github_avatar` (owner extraction, .png?size=256 format, None/empty/non-github→None), `get_favicon_url` (google service, domain injection, sz=128). (b) `test_enrich_batch_format_prompt.py` (12 tests) — `format_prompt_for_batch` (2-message structure, name/desc injection, tags list→join, title fallback, unknown type no-crash, different types produce different prompts). (c) `test_assign_to_clusters_helpers.py` (10 tests) — `build_clusters_from_assignments` (min_cluster_size per content_type, carousel cap/sort, secondary dedup, None/error primaries ignored, unknown cluster no-crash). (d) Extended `tests/js/submit-worker-helpers.test.js` (+25 tests): exposed `mapToSchema` and `getCategories` — `mapToSchema` (package github_url detection, paper→title not name, paper.citations=0, learning.domain extraction, book.isbn, community.location/dates, all types get _submitted, whitespace trim, default fallback), `getCategories` (all 7 types present, non-empty arrays, strings only, community includes Conferences). Suite: 503 JS (+16), 1306 Python (+47).

## 2026-05-25 (loop iteration 19)
- **Test expansion — migrate_data + narrative_carousels + enrich_metadata_v2 + recategorize_talks + postprocess_clusters + reorganize_resources**: (a) `test_migrate_data_helpers.py` (28 tests) — `get_schema_version` (dict/list/None, non-int fallback), `stamp_meta` (dict/list wrapping, immutability, ISO timestamp, preserves extra _meta keys), `find_migrations_for` (already-at-target, backwards raises, chain walking, wildcard `*` target) — fixed test isolation issue using `monkeypatch` for module-level `MIGRATIONS` list, `migrate_payload` (no-op, chain apply+stamp). (b) `test_generate_narrative_carousels_helpers.py` (27 tests) — `find_items_by_tags` (tag/category/type filter, case-insensitive, substring), `find_items_by_text` (name/desc match, min_matches, _match_score sort), `select_diverse_items` (round-robin, max_count, single type), `has_person_name` (full name, last name only). (c) `test_enrich_metadata_v2_helpers.py` (28 tests) — `compute_hash` (deterministic, 16-char, extra fields ignored), `get_item_id` (name>title>url>unknown), `needs_enrichment` (force/unknown/hash-mismatch/schema-version/wrong-key), `calculate_confidence` (question count, summary length, difficulty validity, audience validity, paper/package/dataset type-specific penalties). (d) `test_recategorize_talks_helpers.py` (13 tests), (e) `test_postprocess_clusters_helpers.py` (14 tests) — `cosine_similarity` (identical/orthogonal/opposite/scaled), `compute_hero_score` (type routing, recency decay, citation boost, star boost). (f) `test_reorganize_resources_helpers.py` (15 tests) — `transform_resource` (category mapping, type normalization, macro_category assignment, difficulty normalization, domain backfill). Also updated CLAUDE.md: Python test count 1120→1259. Suite: 487 JS, 1259 Python.

## 2026-05-25 (loop iteration 18)
- **Test expansion — check_secrets + analyze_experiments + generate_hero_images + discover_content**: (a) `test_check_secrets_helpers.py` (14 tests) — `parse_keys` (UPPER_SNAKE_CASE extraction, comment/blank skip, export prefix, nonexistent file→empty), `diff_keys` (missing/extra, sorted, empty template). (b) `test_analyze_experiments_helpers.py` (25 tests) — `_phi` (CDF properties, symmetry), `wilson_ci` (unit interval, width shrinks with n, z widens), `two_prop_z` (equal→p=1, sign direction, large samples significant), `_verdict` (insufficient data guard at n<100, p-value thresholds). (c) `test_generate_hero_images_helpers.py` (17 tests) — `slugify` (unicode NFKD, max-80, empty fallback), `build_prompt` (name/category/type injection, no-text constraint, semantic_cluster fallback). (d) `test_discover_content_helpers.py` (16 tests) — `validate_item` (required fields, URL, name length, tags type, year range, workshop exclusion), `get_weekly_queries` (budget cap, underscore-skip, rotation). Suite: 487 JS, 1120 Python.

## 2026-05-25 (loop iteration 17)
- **Test expansion — enrich_metadata prompt structure**: `test_enrich_metadata_prompt.py` (11 tests) — `get_prompt` structural tests: name/desc/category field injection, item_type uppercasing, tags list→comma-string join, JSON schema fields present (`difficulty`, `topic_tags`, `synthetic_questions`), empty item no-crash, `title` field fallback for papers, different item types produce different prompts. Suite: 487 JS, 1048 Python.

## 2026-05-25 (loop iteration 16)
- **Test expansion — image script helpers**: `test_image_script_helpers.py` (25 tests across 4 helpers): (a) `slugify` from `download_blogger_images.py` (basic/special/no-trailing-hyphen/lowercase), (b) `get_extension` (png/jpg/jpeg/webp/svg from URL; content-type fallback; default .jpg) — discovered `.jpeg` URLs return `.jpeg` not `.jpg` (no normalization), (c) `extract_youtube_thumbnail` from `fetch_og_images.py` (watch/short/embed URL patterns, channel→None, non-youtube→None), (d) `get_root_domain` from `fetch_logo_fallbacks.py` (www-strip, subdomain stripping, co.uk TLD). Suite: 487 JS, 1037 Python.

## 2026-05-25 (loop iteration 15)
- **Test expansion — categorize scripts**: `test_categorize_helpers.py` (16 tests) — `categorize_blogger` (causal/ml/experiment/growth/platform/economics keyword routing + default fallback) + `get_sector` (marketplace/streaming/adtech/fintech/creator/OR keyword routing + fallback). Both functions in single test file via two-module load pattern. Suite: 487 JS, 1012 Python.

## 2026-05-25 (loop iteration 14)
- **Test expansion — build_engagement_scores + generate_highlight + generate_embeddings + update_stars**: (a) `test_rank_all_content_freshness.py` extended: added `TestBuildEngagementScores` (7 tests) — clicks scoring, name lowercasing, null-safe count, search_clicks nested JSON format, signals raw-count tracking. Discovered return is 3-tuple `(scores, signals, cooccurrence)`. (b) `test_generate_embeddings_helpers.py` (17 tests) — `combine_text_for_embedding` (rich embedding_text path, legacy fallback, None-safe list fields, category label) + `slugify` (max-100, consecutive special, empty). (c) `test_update_stars_helpers.py` (10 tests) — fixed `extract_repo_info` bug: `.rstrip(".git")` strips individual chars {., g, i, t}, not the literal suffix (silently truncated repo names like "project" → "projec"); fixed with `re.sub(r'\.git$', ...)`. (d) `test_generate_highlight_helpers.py` (13 tests) — `select_topic`, `find_related_packages`. (e) `test_sync_resources_helpers.py` (15 tests). Suite: 487 JS (unchanged), 996 Python.

## 2026-05-25 (loop iteration 13)
- **Test expansion + bugs found — generate-highlight + tracker**: (a) `test_generate_highlight_helpers.py` (13 tests) — `select_topic` (override by id/name case-insensitive, all-covered reset, uncovered filtering), `find_related_packages` (keyword match, 4-char minimum, max-5 cap). (b) `tracker-helpers.test.js` (11 tests) — `_hasAnyAssignment` (null/empty/one/multi), `_getWordCount` (spaces/newlines/empty). Added `window.Tracker._*` test surface to `static/js/tracker.js`. Suite: 487 JS, 989 Python.

## 2026-05-25 (loop iteration 12)
- **Bug fixes + test expansion — split_book, sync_resources, generate_embeddings, update_stars**: (a) Fixed two bugs in `split_book.py`: `CHAPTER_PATTERN` missing `re.MULTILINE` (making it unable to match chapter headers past line 1), and `split_chapters` discarding preamble content before first heading (front matter was silently dropped). Tests caught both. (b) `test_split_book.py` (25 tests) — `slugify`, `pick_split_level`, `detect_chapter_boundaries`, `split_chapters`. (c) `test_sync_resources_helpers.py` (15 tests) — `normalize_url` prefix stripping, case, trailing slash, idempotency. (d) `test_generate_embeddings_helpers.py` (17 tests) — `combine_text_for_embedding` (rich embedding_text path, legacy fallback, field assembly), `slugify`. (e) Fixed bug in `update_stars.py`: `.rstrip(".git")` strips individual characters {., g, i, t} not the literal suffix, silently truncating repo names like "project" → "projec". Fixed with `re.sub(r'\.git$', '', ...)`. (f) `test_update_stars_helpers.py` (10 tests). Suite: 506 JS (unchanged), 909 → 976 Python.

## 2026-05-25 (loop iteration 10)
- **Test expansion — submit-worker + rank_content + creative names + rank_all_content extras**: (a) `submit-worker-helpers.test.js` (24 tests) — first coverage of submit-worker pure functions: `isValidUrl` (http/https allow, ftp/js: reject), `extractDomain` (www-strip, port-strip via hostname not host — documented as test comment), `validateSubmission` (all resource types, all required fields, length limits, multi-error accumulation). (b) `test_rank_content_helpers.py` (21 tests) — `wilson_lower_bound` (unit-interval invariant, larger-sample tighter bound, z-score sensitivity), `normalize` (empty, all-equal, negative values, length preservation), `log_scale` (zero→0, 100→~1, monotonic). (c) `test_generate_creative_names.py` (17 tests) — `is_valid_creative_name` full blacklist coverage + word-count bounds. (d) `test_rank_all_content_freshness.py` extended: 28→47 tests, added `normalize_scores`, `safe_join` (None/list/str/int coercion), `apply_citations_boost` (paper type gate, cap at 1.0, baseline for unranked papers). Suite: 482 → 506 JS (23 files), 852 → 909 Python.

## 2026-05-25 (loop iteration 9)
- **Test expansion — analytics worker + 3 more test sets**: (a) `analytics-worker-helpers.test.js` (26 tests) — first-ever coverage of analytics worker pure functions: `sortAndLimit` (sort+limit by count), `hashIP` (base-36 deterministic hash), `isAllowedOrigin` (ALLOWED_ORIGINS whitelist), `aggregateKVEvents` (KV stats aggregation including session dedup, click bucketing, search query lowercasing, country aggregation). Used regex-strip of `export default {...}` block to expose functions to jsdom `window`. (b) `test_build_site_scoreboard.py` expanded: 16→34 tests, added `_try_float`, `_try_int`, `_shorten_path` micro-helpers. (c) `test_rank_all_content_freshness.py` expanded: 10→28 tests, added `extract_item_name_from_path` (URL path → item name, hyphen/underscore normalization) and `extract_url_domain` (github/arxiv/youtube/kaggle/medium/substack/other classifier). Suite: 456 → 482 JS (22 files), 816 → 852 Python.

## 2026-05-25 (loop iterations 7-8)
- **Test expansion — 3 new test files, 2 new expose surfaces**: (a) `test_convert_readme.py` (33 tests — parse_links markdown URL extraction incl. git/docs/pypi text heuristic, generate_tags all 24 categories + keyword matching + 4-tag dedup cap, parse_readme table parsing incl. bold stripping/section skipping/PyPI fallback URL). (b) `test_enrich_batch_slugify.py` (11 tests — slugify max-80-char cap, unicode, consecutive special collapse, underscore→hyphen; stubbed enrich_metadata_v2 + openai to avoid heavy deps). (c) `test_generate_homepage_rows.py` expanded: 13→35 tests, added `apply_type_cap`, `dedup_against_used`, `mark_used`, `build_score_lookup`, `make_item_from_meta`. (d) `explore.js` — added `window.Explore._*` test surface (slugify, isNewItem, isPopularItem, getDailyHeroSeed, selectDailyHero, truncate, getEngagementBadge); `explore-helpers.test.js` (34 tests). (e) `analytics-dashboard.js` — added `window.AnalyticsDashboard._*` test surface (formatNumber, formatTime); `analytics-dashboard-helpers.test.js` (13 tests). Suite: 409 → 456 JS (21 files), 750 → 816 Python.

## 2026-05-25 (loop iteration 6)
- **Test expansion + CI integration fix**: (a) 4 new Python test files: `test_cluster_hdbscan.py` (35 tests — get_section_from_id, normalize_word, format_ctfidf_label with SKIP-word filtering, combine_labels parent/suffix dedup), `test_cluster_topics.py` (17 tests — generate_clean_label tag dedup/hierarchy stripping, dedupe_labels numeric fallback), `test_postprocess_clusters.py` (21 tests — cosine_similarity, is_career_cluster, is_generic_label, get_career_industry). (b) CI fix: `validate.yml`, `recsys-ci.yml`, `discover-content.yml` all called `python scripts/validate_data.py` without `--skip-links`, causing every data-change PR to make hundreds of network HEAD requests (timing out at 60s+ locally, burning 5+ minutes in CI). All three workflows now use `--skip-links`; the separate `check-links.yml` handles weekly link audits. Suite: 409 JS, 639 → 712 Python.

## 2026-05-25 (loop iteration 5)
- **Test expansion + integration improvements**: (a) 2 new Python test files: `test_cluster_resources.py` (24 tests — get_resource_id slug generation + format_label abbreviation handling), (b) 1 new JS test file: `tracker-helpers.test.js` (30 tests — jaccard similarity, hash, truncate, getReferrerSource; exposed 4 helpers via `_*` test surface on `window.Tracker`). (c) `tracker.js` test surface expanded: `_jaccard`, `_hash`, `_truncate`, `_getReferrerSource` added alongside existing `_getExperimentAssignments`/`_hasAnyAssignment`. (d) `validate_data.py` now accepts `--skip-links` flag so CI can run data integrity checks (steps 1-3) without the network link-check that was timing out at 60s+. (e) Data integrity confirmed clean: 0 field errors, 0 sync errors, 0 duplicate URL errors across 42 data files. Suite: 379 → 409 JS (19 files), 615 → 639 Python.

## 2026-05-25 (loop iteration 4)
- **Deeper test coverage + additional bug fixes**: (a) 4 new Python test files: `test_enrich_metadata_v2.py` (20 tests — compute_hash, get_item_id, needs_enrichment, calculate_confidence), `test_discover_content.py` (21 tests — validate_item gate for all content types), `test_check_links.py` (14 tests — extract_urls recursive URL collector), fixtures revealed discover_content actually requires description/tags/language for packages (more complete than docs suggested). (b) 3 new JS test files: `query-parser.test.js` (35 — fixed fieldRegex lastIndex corruption bug; multi-filter queries now work), `search-synonyms.test.js` (18 — SYNONYMS data integrity: every key is lowercase, every value is non-empty array), `search-cache.test.js` (21 — isExpired/isValidVersion/wrapWithMeta pure helpers + KEYS integrity). Suite: 358 → 379 JS, 539 → 594 Python.

## 2026-05-25 (loop iteration 3)
- **Search module tests + data integrity fixes**: (a) 3 new JS test files: `tests/js/query-parser.test.js` (35 tests — found and fixed real bug: `fieldRegex.exec()` + mid-loop `remaining.replace()` corrupts `lastIndex`, causing only first field filter to be captured when multiple `author:X year:Y` style filters used), `tests/js/search-synonyms.test.js` (18 tests — data integrity check on 200+ SYNONYMS entries), `tests/js/favorites-local.test.js` / `playlists-local.test.js` (already in iter 2, now 23+26 tests). (b) `validate_data.py` duplicate-URL logic fixed for `papers_flat.json`: was using broad `topic` field in composite key, causing legitimate cross-subtopic paper listings to fail; now uses `category` (`topic > subtopic`) for precise keying. (c) `test_recsys_outputs.py` gets 6th invariant `content_files_sorted_by_score` — catches any content file where inject_scores.py was not called after manual edits. Suite: 305 → 358 JS, 538 → 539 Python. Hugo: 108 pages.

## 2026-05-25 (loop iteration 2)
- **Test expansion + data fixes**: (a) 3 new JS test files: `tests/js/reading-history.test.js` (23 tests, localStorage mock pattern), `tests/js/favorites-local.test.js` (23 tests, exposed and fixed Date.now() ID collision bug in `playlists-local.js`), `tests/js/playlists-local.test.js` (26 tests). (b) 4 new Python test files: `tests/python/scripts/test_validate_data.py` (26 tests, including 7 for new `check_papers_sync`), `tests/python/scripts/test_inject_scores.py` (8 tests, subprocess-based), `tests/python/scripts/test_flatten_papers.py` (17 tests, covers `slugify`, nested flatten, ID dedup). (c) `validate_data.py`: added `check_papers_sync()` — counts papers in nested `papers.json` vs flat `papers_flat.json`, fails CI if they diverge. (d) Found and fixed real data desync: `papers.json` had 1377 papers but `papers_flat.json` had only 1090 — ran `flatten_papers.py` then `inject_scores.py` to regenerate. (e) Bug fix in `playlists-local.js`: `Date.now()` ID was collision-prone; now `'playlist-' + Date.now() + '-' + Math.random().toString(36).slice(2,7)`. Suite: 256 → 305 JS, 514 → 538 Python.

## 2026-05-25 (loop iteration 1)
- **Integration sweep + Re1 MMR wiring**: (a) Fixed `static/js/analytics-dashboard.js:27` — stale v1 worker URL replaced with v2 (`tech-econ-analytics-v2.pp712.workers.dev`). (b) Added `window.Dashboard` test-surface export to `dashboard.js` and wrote 36 vitest tests covering `fmtNum`, `pct`, `escHtml`, `skeletonHTML`, `renderTraffic`, `renderTopContent`, `renderSearch`, `renderModels`, `renderExperiments`. (c) Wired Re1 MMR experiment (A.3): `generate_homepage_rows.py` now loads `search-embeddings.json` and writes `items_mmr` (MMR λ=0.7 reordered) alongside `items` for every row; `home.html` dual-renders control/treatment divs per standard row; new `static/js/homepage-mmr-experiment.js` (~65 lines) toggles visibility based on `exp_re1_mmr_v1` variant (stays no-op while experiment is draft); `baseof.html` loads the new script. 12 homepage-mmr JS tests + 13 Python tests for `apply_mmr_ordering` / `load_embeddings_lookup`. CI sanity check added to `recsys-ci.yml` (MMR dual-render). Experiment stays `status: draft` pending harness_aa_v2 A/A clearance. Suite: 185 → 233 JS, 474 → 487 Python. Hugo 108 pages clean.

## 2026-05-24
- **PR #51 shipped + pytest fix**: merged as squash `0b504e5`. Test failure was NOT pre-existing as `handoff.md` claimed — commit `8aa2d13` on the same branch added `context=_SSL_CONTEXT` to `urlopen()` in `lib/d1_sessions.py:237` (the certifi macOS fix) without updating the `fake_open(url, timeout)` stub. One-line fix: `def fake_open(url, timeout, context=None):` in `tests/python/lib/test_d1_sessions.py`. 466+7+1 → 474 pass. New rule added to `.claude/RULES.md` §2: verify handoff/PR-readiness claims against the actual CI log before merging.
- **Homepage polish round 2**: four changes shipped together. (a) Homepage row order rewritten in `scripts/generate_homepage_rows.py` — Talks Worth Watching and a new Blogs & Tutorials row (type=resource) now sit right after the hero, before New This Month / Top Packages / Top Datasets. `data/homepage_rows.json` regenerated: 5 rows → 6 rows, 42 items → 50 items. (b) Difficulty tags removed from all five rendered surfaces: `layouts/learning/list.html` (level filter pills + card-footer level badges), `static/js/search/unified-search.js` (search-result difficulty badge, advanced-prerequisites branch, sidebar facet filter dropdown), `layouts/papers/single.html` + `layouts/packages/list.html` (pagefind `data-pagefind-filter="difficulty:..."` attributes). Filter-state code in unified-search.js left in place — dormant without UI surface. `difficulty` field stays on the 4,180+ items in source data files for possible future use. CSS `.difficulty-*` and `.learning-card-level.*` classes left as dead selectors for a future hygiene sweep. (c) Hero spotlight now consumes `item.image_url` automatically when present (in addition to the existing `featured.json` `image_override` path). New `.hero-banner-imaged` CSS class with darkening vignette overlay (top→bottom darkens for text legibility, soft side-fade for image-as-border feel) and white-text override that ignores theme. Items in trending-now that already have populated `image_url` (74% of talks, etc.) now render with image when they become the hero. (d) New `scripts/generate_hero_images.py` — DALL-E 3 / gpt-image-1 generation of soft abstract editorial images for top-N items by `model_score`, configurable `--model` and `--size` (defaults gpt-image-1 / 1536x1024). Script is ready to run; not executed this session because the connected OpenAI account hit its billing hard limit. Plan budget of $4 carried forward. Hugo 107 pages clean. npm test 185/185.
- **Roadmap addition — Stream M (Slide deck refresh)** placed in `docs/roadmap.md` Next bucket. Goalpost: `/slides/` (Reveal.js, ~12 hand-written sections, last touched March 2026) rewritten to mirror the eight Under The Hood tabs. M.1 extracts content into a shared `data/site_content.json`; M.2 ranges both /site and /slides over it; M.3 audit. Blocked on Stream C.4 audit (canonical content must be signed off before forking).
- **Stream B.1 — single-item Featured This Week hero (replaces carousel)**: `layouts/_default/home.html` hero block rewritten — 5-item auto-rotating carousel with dots is gone; renders one item with `.hero-banner-spotlight`, 60vh min-height, single CTA. Item picked from `data/featured.json` (new file) via exact `item_name` match across all rows; falls back to top-1 by `model_score` from `homepage_rows.json` `trending-now` when empty (current default — Stefan Wager). Optional `image_override`, `blurb_override`, `cta_text`, `label` override fields supported. Carousel JS IIFE (45 lines) deleted; `.hero-slide` / `.hero-dot` CSS block (60 lines) deleted; mobile bumped 40vh → 50vh, desktop 50vh → 60vh; new `.hero-label` style for the editorial tag. Hugo 107 pages clean. npm test 185/185. validate_data.py exit 0. Carousel residue grep: 0 hits in `layouts/`+`static/` (one Reveal.js `event.currentSlide` in `slides/list.html` is unrelated).
- **Stream B.2 — already satisfied; roadmap marked**: `data/homepage_rows.json` already has 5 rows (target was ≤6); narrative rows (Pat Bajari, Staggered Treatment) no longer exist as standalone rows. No code change.
- **Stream D — already satisfied; roadmap marked**: all four hygiene items (CLAUDE.md:3 roadmap pointer, master_recsys_planner.md:4 journal note, README.md:6 roadmap link, removed "Still TODO §4" block) had already shipped in prior PRs. No code change.
- **Stream C.11 — Frontend design polish on /site and /dashboard**: typography scale (h2 1.75rem Inter Display, h3 1.25rem, body 1rem/1.65), 8-pt spacing tokens, stage-color CSS variables (`--stage-input/gate/enrich/rank/publish` + soft backgrounds), tab bar polish (2.5px active border, 44px tap targets, roving tabIndex), skeleton loaders replace "Loading..." text, keyboard arrow/Home/End nav on both tab bars, 150ms fade-in on tab switch, zebra striping + hover on all tables, tabular-nums on stat numbers, mobile: tab bar horizontally scrolls (no vertical stack), stat grid 2-col. Zero em dashes. Hugo: 107 pages. npm test 185/185.
- **Stream C.10 - Under The Hood artifact enrichment**: Tabs 1-6 of `/site` now include 16 new artifacts across 6 tabs: running examples with real items from the corpus, pull-quotes, conceptual code snippets with actual signal weights, comparison panels, mini SVGs, and a real-data content-count table. New CSS component block appended to `custom.css` (9 new classes, dark mode, mobile). Zero em dashes. Hugo: 107 pages. npm test 185/185.
- **Stream C.9 — /dashboard live page**: new `/dashboard/` route with 5 tabs (Traffic, Top Content, Search, ML Models, A/B Tests). Traffic, Top Content, and Search tabs fetch live from the analytics worker (lazy per tab, memory-cached). ML Models and A/B Tests read from `data/site_scoreboard.json` embedded at build time. Dashboard-specific CSS inlined in the template; reuses `.site-tab*` classes. Nav link added to sidebar (grid icon, after Under The Hood). Hugo build passes (107 pages). Zero em dashes.
- **Stream C.8 — /site Tabs 7+8 (Performance + Experiments)**: added `scripts/build_site_scoreboard.py` (reads metrics.csv, replays.csv, experiments.json + per-experiment markdown reports, writes `data/site_scoreboard.json` atomically); 16 new pytest cases; Tab 7 (ranker eval: NDCG@10 headline + stat cards + sparkline SVG + history table + replay section) and Tab 8 (experiment registry: timeline SVG, active callout, experiments table with status pills, per-experiment detail cards, verdict narrative). Hugo build passes (106 pages). npm test 185/185. pytest 467+16 pass (7 pre-existing d1_sessions stub failures unrelated). Zero em dashes.
- **Stream C.7 — /site human polish pass**: stripped all 12 em dashes from Tab 1 prose, removed every file path, script name, and code object reference from all six tabs (75 total references cleaned), and bumped SVG text sizes (final: headings 20-22px, sub-labels 16-18px, stage labels 14-15px) across all 6 diagrams. Page now reads as editorial blog, not developer documentation. Hugo build passes (106 pages). npm test 185/185.
- **Stream C.6 — /site page Tabs 2-6 shipped**: Storage, Processing, Recsys, How Recs Work, and How A/B Works tabs fully written (400-650 words prose each) with inline SVG diagrams (5-6 nodes each). No em dashes in new content. Stream C now complete pending C.4 audit.
- **Stream A.6 — tracker.js cookie-timing fix + harness_aa_v1 reset**: root cause was `static/js/tracker.js:69-77` deferring the `te_uid` cookie write to `setOnInteraction` (first click/scroll); `experiments.js:getOrMintUid()` reads the cookie eagerly on the first impression, found it empty, and minted a different ephemeral UUID — same user landed in two buckets. Fix: removed the `setOnInteraction` deferral entirely; `te_uid` is now written synchronously in `initUserIdentity()` for new users. `harness_aa_v1` status set to `paused` (contaminated data, 57 users, 19 days). New `harness_aa_v2` added as `active` with same 50/50 A/A structure and `start_date: 2026-05-24`. New regression test at `tests/js/tracker-cookie-timing.test.js` (6 tests). `aa-experiment-config.test.js` updated to assert v2 active + v1 paused. Suite: 175 → 185 tests, all green.
- **Stream B — Editorial polish (B.3, B.4 partial)**: extended Inter Display from hero-only to all card `h3` titles (`.explore-card h3`, `.explore-card-hero h3`, `.explore-card-compact h3`) and added subtle 4%-opacity type-tinted card backgrounds keyed by `data-type` attribute (package=blue, dataset=green, paper=orange, resource=purple, talk=orange, career=teal, community=pink, book=green). Dark mode resets tint — left-border accent already provides the signal. Pure CSS append to `custom.css`. No JS, no data changes. B.5 HITL sign-off needed before merge.

## 2026-05-23
- **`docs/roadmap.md` (NEW)**: living Now/Next/Later roadmap. Frames repo as a learning vehicle for end-to-end recsys + AI-agent dev. 12 streams across 3 horizons, audit-gate ritual, dual tech+human metrics, cross-link map. `CLAUDE.md`, `README.md`, `master_recsys_planner.md` updated to point here for forward planning.
- **CLAUDE.md hygiene**: stale §7 "Still TODO (server-side)" block removed — all three items shipped in PRs #46–48. Section now reflects current state + points to roadmap Stream A for open items.
- **Stream A.5 audit — `harness_aa_v1` A/A test is BROKEN**: ran `analyze_experiments.py` for the first time. Two no-op variants showed `control_a=8,071 imp / 3.99% CTR` vs `control_b=10,205 imp / 2.32% CTR`, z=-6.50, p≈0. Root cause: `static/js/tracker.js:69-77` lazy-writes `te_uid` cookie (waits for first interaction); `static/js/experiments.js:88-104` reads it eagerly on first impression. First-visit users bucketed twice — ephemeral UUID for impressions, real UID for later events. 57 distinct users appeared in both variants. 19 days of A/A data contaminated. **Fix tracker.js cookie timing + reset experiment is HITL-blocked (Stream A.6).**
- **Script fixes (unblocks the analysis tooling)**: `scripts/analyze_experiments.py`, `scripts/rank_all_content.py`, `lib/d1_sessions.py` — wrangler stdout preamble (`"Cloudflare agent skills are available for..."`) broke `json.loads`; now regex-extract `\[.*\]` (DOTALL). `analyze_experiments.py` raises strict; `rank_all_content.py` warns and returns `[]` (preserves existing contract). `lib/d1_client.py` + `lib/d1_sessions.py` — Python 3.11 macOS doesn't trust system keychain; added `certifi.where()` via `ssl.create_default_context` at module import, soft dep (falls back on Linux CI).
- **First-ever row in `reports/replays.csv`**: `python3 scripts/replay_eval.py --candidate data/global_rankings.json` completes. ndcg@10=0.2275, 145 sessions loaded, 39 evaluable. Seeds the gate toward the ≥3-rows threshold for Stream F (Ra2 knn-bge default flip).
- **Stream C — `/site` transparency page skeleton shipped**: `content/site/_index.md` + `layouts/site/list.html` (Hugo picked list.html by default). 6-tab bar in place. **Tab 1 Ingestion** fully built: 580 words + 5-node inline SVG (Sources → Validation → Enrichment → Ranking → Published), cites `validate_data.py` / `enrich_metadata.py` / `rank_all_content.py` / submit-worker. Tabs 2–6 stubbed with "Coming soon" + Stream pointer. CSS: `.site-tabs / .site-tab / .site-tab-content / .site-diagram-container / .site-prose / .site-coming-soon` in `static/css/custom.css` (~120 lines, mobile-responsive). Nav link "How It Works" added to `layouts/_default/baseof.html` sidebar with info-circle icon. `hugo --gc --minify` passes (106 pages, 0 new warnings). Two follow-ups for Stream H: SVG fills are inline hex (no dark-mode adapt), and pre-existing `--accent-color` is undefined in career-tab CSS (silent bug, not introduced here).

## 2026-05-04
- **`scripts/check_secrets.py`**: small helper that compares `.claude/secrets.env.template` against the user's actual `.claude/secrets.env` and exits non-zero if any template-declared key is missing locally. Closes the gap that bit during today's worker deploy: `ADMIN_KEY` was in the template but not in `secrets.env`, so `/run-schema?key=$ADMIN_KEY` returned 401. Pure read-only; never echoes values; 17 new pytest cases covering parse / diff / main exit codes.
- **First real experiment shipped: `harness_aa_v1` A/A test (`data/experiments.json`)**: closes audit §4.A7 step 1. Both variants do the same thing (`control_a` / `control_b`, 50/50). Sole purpose is to validate the end-to-end harness — bucketing → tracker `event.exp` → worker `events.experiments` → `analyze_experiments.py` — by checking traffic actually splits ~50/50. New `tests/js/aa-experiment-config.test.js` runs 10k synthetic uids against the actual config and asserts pctA ∈ [47%, 53%] (4 new tests; suite 171 → 175). Real treatment-vs-control ranker experiments come next.
- **`scripts/analyze_experiments.py` — A/B per-variant CTR + significance (PR #47)**: closes the §4 server-side trio (#42 client → #46 worker schema → this script reads the data). Per active/paused experiment in `data/experiments.json`: pulls per-variant impressions/clicks via `json_extract(events.experiments, '$.<exp_id>')`, prints a stdout summary, writes a markdown report to `reports/experiments/<exp_id>-YYYY-MM-DD.md` with Wilson 95% CIs, two-proportion z-test vs control, and the SQL behind the result. Verdict heuristic: ≥100/arm minimum, p<0.01 for "wins", p<0.05 for "weak signal". Pure subprocess to wrangler (no new deps); 31 new pytest cases covering stats helpers, render paths, count parsing, CLI date coercion.
- **§4 server-side scaffolding step 2 (PR #46) — `analytics-worker/index.js`**: adds `events.experiments` column (JSON map of `{experiment_id: variant_id, ...}`) plus a sparse index on `experiments IS NOT NULL`. INSERT path serializes `event.exp` from the tracker payload (PR #42); migration mirrored in `ensureSchema` (per-isolate self-heal) and `handleRunSchema` (cross-isolate idempotent ALTER) per RULES.md F15. **Deploy applied** (`npx wrangler deploy` + direct `ALTER TABLE` via wrangler since `ADMIN_KEY` wasn't in the local `secrets.env`); end-to-end smoke test succeeded (`json_extract(experiments, '$._example_disabled')` returned `[{"variant":"control","n":1}]` against a probe event).
- **Move `FRESHNESS_WEIGHT` / `FRESHNESS_HALF_LIFE_DAYS` constants out of `rank_all_content.py` into `data/recsys_config.json`**: closes the deferred A3 follow-up from PR #17. Constants deleted from the module; `calculate_freshness_scores` now reads `config.ranking.freshness_boost_max` and `config.ranking.freshness_half_life_days` via `_load_recsys_config()`. New optional `config=` kwarg lets tests pin behaviour without depending on disk state — also the forward-compat path for per-type half-lives (audit's "papers slow / talks fast"). 2 new pytest cases (`TestConfigOverride`); suite at 410 passed.
- **Migrate `rank_all_content.py:calculate_freshness_scores` to use `lib/freshness.py` (PR #43)**: previously inline; now a thin wrapper around `lib.freshness.compute_freshness_boosts`. Two deliberate behaviour changes documented + pinned: (a) fractional days instead of integer (sub-day precision), (b) clock-skewed future `first_seen` clamps to age=0 instead of producing boost > FRESHNESS_WEIGHT. Both shifts are sub-0.001 magnitude on real data and don't move ranks. 8 new pytest cases (equivalence on integer-day inputs, divergence pin tests, error handling).
- **§4 server-side scaffolding step 1 (PR #42, `static/js/tracker.js`)**: `track()` now reads `window.Experiments.getAllAssignments()` and attaches the result as `event.exp` (a `{experiment_id: variant_id, ...}` map) when non-empty. Worker-side ingestion is intentionally still TODO (next PR adds the schema + INSERT path); current worker harmlessly ignores unknown event fields, so this is forward-compat scaffolding. 13 new vitest cases covering the helper and `track()` integration (suite 158 → 171).
- **Re4 session-aware dampening (PR #37, extends `static/js/personalize.js`)**: items the user has already clicked (sources of the Ra4 boost) get pushed DOWN in their row with multiplier `1 - DAMPEN` (0.8). Dampening **trumps** boosting: a card that is both clicked AND a neighbour of another clicked item still gets dampened. New `buildDampenSet` helper; `reorderRow` gains a 4th optional `dampen` arg (3-arg call still works). 12 new tests (suite 116 → 128).
- **`lib/freshness.py` (PR #17)**: extracts `FRESHNESS_WEIGHT` and `calculate_freshness_scores` from `rank_all_content.py` into a tested module (31 tests). API accepts a `Mapping[type → half_life_days]` so future per-type half-lives (papers slow, talks fast) become a config swap, not a code change. Pure-add — call sites in `rank_all_content.py` not yet migrated; that lands separately with a side-by-side comparison.
- **`lib/d1_client.py` (PR #13)**: HTTP wrapper around the analytics worker's read endpoints (`/health`, `/stats`, `/clicks`, `/searches`, `/timeseries`, `/clicks-by-country`). Replaces the wrangler-CLI subprocess pattern. Read-only; writes still go through the worker's POST `/events`. Injectable `FakeFetcher` for tests (33 unit tests, no real HTTP). Frozen `D1Response` dataclass exposes status / url / elapsed_ms.
- **Ra4 client-side personalization (PR #36, `static/js/personalize.js`)**: multiplicative re-rank of homepage `.cards-row` cards by the user's reading history. For each of the last 5 history items, top-5 neighbours from `related-items.json` get a rank-decayed boost (1.0 → 0.6); each card's static `model_score` is multiplied by `1 + 0.2 × max(boost)`. Stable on ties, runs on `requestIdleCallback`, no-ops with `< 3` history items or any fetch failure. Reuses the 1.4 MB `related-items.json` already loaded by `because-you-viewed.js` instead of pulling the 16 MB raw embedding binary the original audit assumed (`search-cache.js:getEmbedding(id)` doesn't exist — only blob-level access).

## 2026-05-03
- **Eval gate hardening (PR #31)**: auto-prefer HTTP `/events-raw` when `ADMIN_KEY` is set, fall back to wrangler on failure; skip regression check when prev row's `holdout_days` differs from new run (apples-to-oranges). 4 new tests, suite at 318.
- **Eval gate skips on `n_evaluable=0` (PR #30)**: post-blackout reruns immediately had impressions but no clicks → metrics=0 → false-positive 100% regression. Gate now treats no-click holdouts as "no signal" and skips both the comparison and the row write.
- **`/events-raw` worker endpoint (PR #27, deployed)**: ADMIN_KEY-protected GET endpoint backing the eval gate's HTTP path. Worker deployed via `npx wrangler deploy`; analytics blackout self-heal active (`events_24h: 107` and growing).
- **Wired `inject_scores.py` into the rerank (PR #29)**: `update_rankings.sh` was running `rank_all_content.py` then skipping `inject_scores.py` entirely; `model_score` on every item in `data/{books,career,community,datasets,packages,papers_flat,resources,talks}.json` had been frozen at 2026-03-30. Hugo templates read `model_score` from the source files, so the homepage's per-category ordering was stale for 5 weeks. Fixed + refreshed all 9 source files.
- **Replay history CSV + structured notes (PR #28)**: `replay_eval.py` now appends `reports/replays.csv` per run (verdict, deltas, baseline/candidate paths). `metrics.csv` notes auto-include `cold_start_method` so future readers can tell regression rows from knn-bge rows.
- **Skip regression-train on knn paths (PR #26)**: `--cold-start-method=knn-tfidf|knn-bge` no longer trains the SBERT regression model (4 min → 19 sec, 13× speedup).
- **Ra2 v2: `--cold-start-method=knn-bge` (PR #25)**: BGE-large-en-v1.5 (1024d) k-NN cold-start. A/B vs regression baseline: -4.1% NDCG@10, identical Hit-Rate@10 — within noise on 15 sessions. Kept opt-in.
- **Ra2 v1: `--cold-start-method=knn-tfidf` (PR #24)**: TF-IDF k-NN cold-start. A/B regressed 10% across the board. Kept opt-in for future BGE-via-TF-IDF comparisons.
- **First metrics.csv row (PR #23)**: Seed rerun NDCG@10 = 0.4191, Hit-Rate@10 = 0.8000 over 15 evaluable sessions, 60-day holdout. The recsys scoreboard's zero-point.
- **Eval-gate fixes (PR #22)**: accepted `'impression'` event type (worker writes full word, not abbreviated 'impress'); switched the gate to wrangler subprocess (the HTTP endpoint hadn't shipped yet); `update_rankings.sh` tolerates missing `reports/metrics.csv`.
- **MMR diversification on homepage trending row**: `scripts/rank_all_content.py:select_diverse_trending` now uses `lib.diversity.mmr_rerank` over SBERT embeddings (lambda=0.7, matches search-side). Replaces the legacy "max-2-per-type, max-2-per-category" rule. 15 new tests; full suite at 300 green.
- **Phase 1 eval pipeline complete** (planner step "Offline evaluation pipeline"). New: `lib/d1_sessions.py` (group D1 events by session), `lib/eval_runner.py` (orchestration + atomic CSV append + regression check), `scripts/evaluate_recsys.py` (CLI runner that appends to `reports/metrics.csv`), `scripts/replay_eval.py` (baseline-vs-candidate side-by-side). `scripts/rank_all_content.py` now calls the eval gate after building rankings and before writing `data/global_rankings.json` — if NDCG@10 drops more than `config.evaluation.ndcg_drop_alert_threshold` (default 5%), the rerank exits 5 and production scores are untouched. 51 new tests; full suite at 285 passing.
- **Rerank**: Refresh global rankings with latest D1 engagement (518 items with signals, 309 with clicks); homepage trending repopulated (12 items, was empty); regenerate `homepage_rows.json`. Top item: "Causal Inference: A Statistical Learning Approach" (73 clicks).
- **Analytics blackout fix**: Diagnosed 5-week silent write failure on the v2 worker (`events.user_id` column missing — schema migration never applied). Permanent code fixes: `handleRunSchema` now self-heals the user_identity migration idempotently; `processEvents` no longer wraps the events batch and `updateAggregates` in one try/catch (a schema mismatch on events used to silently kill all aggregate writes too); `/health` now reports `last_event_ts`, `last_write_age_seconds`, `events_24h`, `write_errors_today`, with `status=degraded` when stale; rerank script aborts if `/health` is degraded (`--ignore-stale` to override). Runbook added to `claude.md`. Migration application + deploy pending wrangler reauth.

## 2026-03-26
- **Weekly reranking pipeline**: `/ranking-export` API endpoint on analytics worker, `--source=api` mode for ranking script, scheduled remote trigger (Monday 6am ET)
- **Netflix-style homepage**: Rotating hero carousel, card images from data, career items removed, dramatic gradient
- **Card images**: GitHub org avatars for packages, Open Library covers for books, image_url pipeline in homepage rows
- **Data quality**: Fix row sizing (min 5, max 10), analytics section mapping for resources/community
- **MCP search**: Tokenized multi-field scoring algorithm overhaul
- **Homepage row validation**: Fix type cap in toolkit builder, padding for short narrative carousels, citation-based paper scoring
- **UI overhaul**: Netflix-quality cards, type-specific colors, Inter font, search animations, bigger cards

## 2026-03-25
- **Homepage layout**: Simplify navigation, remove redundant section grid/CTAs/Discover tab
- **Search UX**: Pass authors, year, tags, format through search worker results; type metadata in results

## 2026-02-07
- Add 6 causal inference/ML packages: grf_python, causalfe, etwfe, diff-diff, choice-learn, GeoAI
- Add Stefan Wager's "Causal Inference: A Statistical Learning Approach" book
- Homepage: rename "Trending Now" → "New Additions" with star icon, showcase new items

## 2026-01-16
- Add choix, pyStoNED, Prest packages for preference modeling and revealed preference analysis

## 2026-01-11
- **Analytics system improvements** - stop losing data, use more signals:
  - Added `web_vitals` table (LCP, FID, CLS) - was losing 2,912 events
  - Added `client_errors` table - was losing 93 error events
  - Added `referrer_stats` table - classify traffic sources (google, twitter, hackernews, etc.)
  - Added device classification (mobile/tablet/desktop) to tracker
  - Populated `item_cooccurrence` from session sequences
  - Added rate limit truncation logging
- **Ranking model improvements**:
  - Added `reading_ratio` signal (actual vs expected read time)
  - Added `high_imp_no_click` negative signal (impressions ≥10 with 0 clicks)
  - Added engagement features to model (CTR, has_clicks, log_clicks, etc.)
  - Comprehensive target using all 13 signals
- **UX fix**: Scroller nav buttons now 60% visible (was 0% causing rage clicks)
- Updated claude.md with new signals, tables, and analytics queries

## 2026-01-06
- Added 4 revealed preference datasets: Chicago Taxi (labor supply), Irish CER Smart Meter (RCT ToU pricing), Uniswap DEX (DeFi trading), iFlex Norway (hourly electricity)
- Added 4 dunnhumby Source Files datasets: Breakfast at the Frat (time series), Carbo-Loading (household panel), The Complete Journey (full marketing mix), Let's Get Sort-of-Real (300M synthetic transactions)
- Added 3 revealed preference packages: InvOpt, PyInvo (inverse optimization), revealedPrefs (GARP/WARP testing)

## 2026-01-05
- Added ML Pipeline documentation to claude.md (ranking, search, clustering, discovery, enrichment, ALS)
- Added UI philosophy: large cards with images, rich metadata
- Created slash commands: `/rerank`, `/review-clusters`, `/enrich`
- Set up `.claude/history/` for changelog and claude.md archives
- **Package clustering with Netflix-style carousels**: 38 topic clusters for 188 packages
  - Full cluster-by-cluster review documented in `scripts/CLUSTERING_REVIEW.md`
  - Fixed 5 misassignments: SuperLearner→pkg-tmle, collapse→pkg-data-manipulation, stargazer→pkg-visualization, inferference→pkg-causal-mediation, linregress→pkg-hypothesis-testing
  - Cluster categories: Causal Inference (10), Experimentation (4), ML (6), Ops Research (5), Time Series (3), Statistics (8), Industry (9), Data Utils (3)
- **Learning section niche clusters V4**: Fixed clustering coherence issues
  - 54 clusters, all in 4-10 item range (avg 6.6 items)
  - Fixed "Agent Based Modeling" clusters - now contains only actual ABM content
  - Similar small semantic_clusters grouped together (similarity > 0.75) instead of merged into random large clusters
  - Industry Economics now has coherent clusters: "Price Theory", "Dynamic Programming", "ABM"
- Updated `scripts/cluster_resources.py` Phase 2: group similar small clusters together instead of merging into large ones
- Re-scored rankings with latest engagement data (1,316 items with engagement)

## 2026-01-04
- Added HDBSCAN clustering script for topic organization (per-section, c-TF-IDF labels, recursive splitting)
- Fixed redundant cluster labels (94→1 issues) with word-level deduplication
- Re-scored rankings with latest engagement data (1,295 items with engagement, 2,540 cold-start)
- **Deep Learning + Statistical Models landscape** (~43 new entries):
  - 18 datasets (dSprite, MIMIC-CXR, TCGA, French MTPL, GHCN-Daily, etc.)
  - 7 packages (pycox, Pyro, scVI-tools, VaDE, causal-bert-pytorch, etc.)
  - 13 papers (DeepHit, Cox 1972, MC-Dropout, Normalizing Flows, MoE, etc.)
  - 5 resources (conformal prediction, extreme value, survival tutorials)

## 2026-01-03
- **Comprehensive Queueing Theory Resources Directory** (~70 new entries):
  - 7 simulation packages (SimPy, Ciw, simmer, queueing, AnyLogic, Arena, Simio)
  - 8 textbooks (Kleinrock, Harchol-Balter, Gross & Harris, Ross, Hillier, Law, Nelson)
  - 2 conferences (ACM SIGMETRICS, Winter Simulation Conference)
  - 25 resources (MIT courses, industry blogs, calculators, tutorials)
  - 25 papers in new "Queueing Theory & Operations" topic (5 subtopics: Foundational, Ride-Sharing, Call Centers, Cloud/Server, Healthcare)
- Added 31 queueing/operations datasets total:
  - Part 1: 15 Kaggle datasets (call centers, healthcare, server logs, theme parks, flights)
  - Part 2: 16 premium sources (CAIDA, Google Cluster, Technion call center, NYC EMS, etc.)
- New categories: Operations & Service, Technology & Infrastructure, Manufacturing, Telecommunications
- Fetched 28 new dataset images + 22 logo fallbacks (181 total dataset images, 227 logos)
- Added `embedding_text` field (500-1000 words) to LLM enrichment for richer semantic embeddings
- **New clustering/search fields** (4,171 items enriched):
  - `tfidf_keywords`: 10-15 discriminative terms per item
  - `semantic_cluster`: LLM-assigned cluster labels (e.g., "causal-ml-methods", "marketplace-experimentation")
  - `content_format`, `depth_level`: content type and depth filters
  - `related_concepts`, `canonical_topics`: graph edges and controlled vocabulary
- Search index v6: Added new fields with boosts (tfidf_keywords: 2.5, canonical_topics: 2.0, semantic_cluster: 1.8)
- Search metadata v5: New fields now included for client-side filtering and display
- Updated unified-search.js to use index config with new field boosts
- Content-type specific prompts for papers, packages, datasets, resources, talks, career, community
- **Async batch processing**: 10x faster enrichment using asyncio (batch size 10, semaphore rate limiting)
- **OpenAI Batch API**: New `enrich_batch.py` script - 50% cheaper with CLI commands (prepare, submit, status, apply, run)

## 2026-01-02
- **Datasets page Netflix-style redesign**: horizontal scroll rows grouped by category (36 categories)
- Downloaded 153 dataset images locally to /static/images/datasets/ via OG image fetching
- Fallback displays category-colored gradient + 2-letter initials for datasets without images
- Categories sorted by highest model_score item for better content discovery
- Split Blogs tab into "Bloggers" (54 personal) + "Industry Blogs" (126 company) tabs
- Added subtopic categorization for personal bloggers (9 topics: Causal Inference, ML & AI, etc.)
- Added sector-based subtopics to Industry Blogs (10 sectors: Marketplaces, Streaming, Social Media, E-commerce, AdTech, etc.)
- Limited carousel rows to 8 items max for cleaner browsing
- Added Reveal.js portfolio slide deck at /slides/ with cinematic dark theme
- 7 slides showcasing site stats, features, tech stack with animated counters
- Downloaded 54 blogger images locally to /static/images/bloggers/ (previously external URLs)
- Talks page Netflix-style redesign with horizontal scrollers per subtopic
- Carousel rows now sorted by top item's model_score (highest-scoring content first)
- Added macro_category + subtopic fields to talks.json (8 macro categories, 55 subtopics)
- Further granular categorization: Susan Athey Work, Marketplace Case Studies, Chief Economists, etc.
- Created OG image fetching script; 181/264 talks now have thumbnails
- Added analytics D1 query reference to CLAUDE.md for checking recent clicks/impressions/searches
- Fixed 9 INFORMS login-wall links → public URLs (conferences, chapters, datasets)
- Fetched OG images for learning resources (211/366), industry blogs (56/126), conferences (52/109)
- Added logo fallback fetching via Clearbit/Google APIs; 143 logos downloaded to /static/images/logos/
- Final image coverage: Learning 73% (268/366), Industry Blogs 60% (76/126), Conferences 79% (87/109)

## 2026-01-01 (Learning page Netflix-style redesign)
- Reorganized resources.json: 64→48 categories, 80→10 types, added macro_category field
- Rebuilt /learning with Netflix-style horizontal scrollers per category
- Added filters: macro category (11), type (10), level pills (beginner/intermediate/advanced)
- Cards sorted by model_score within each row, scrollable with nav arrows

## 2026-01-01 (UChicago Causal Inference course)
- Added UChicago "Causal Models in Data Science" course by Jeong-Yoon Lee
- Added 8 industry speaker talks: Facure (Nubank), Lal (Netflix), Zheng (Meta), Chen (Snap), Pan (Snap), Sinha (Lyft), Harinen (Toyota), Mercurio (Netflix)
- Added 2 books: "Causal Inference in Python" (Facure), "Causal Inference for Statistics, Social, and Biomedical Sciences" (Imbens & Rubin)

## 2026-01-01 (Simulation & Synthetic Data content expansion)
- Added ~80 new entries covering simulation, synthetic data, and computational economics
- New packages: Mesa, AgentPy, ABCE, Gymnasium, Stable-Baselines3, RLlib, ABIDES, AuctionGym, CTGAN, Faker, CausalPy, PyMC
- New paper topic: "Simulation & Synthetic Data" with 4 subtopics (ABM, Synthetic Data, Mechanism Design RL, Market Simulation)
- New resources: SFI ABM courses, tech company simulation blogs (Uber, Lyft, Netflix, Airbnb)
- New books: Railsback & Grimm ABM, Epstein & Axtell Sugarscape, Glasserman Monte Carlo

## 2026-01-01 (AI for Economists content expansion)
- Added ~70 new entries for "AI for Economists" content across all data files
- New paper topic: "AI for Economic Research" with 6 subtopics (LLMs, Homo Silicus, Causal ML, Text-as-Data, Satellite Imagery)
- New packages: EDSL, Anthropic SDK, OpenAI SDK, NLTK, sentence-transformers, TensorFlow, 6 research tools (Elicit, Consensus, etc.)
- Added Korinek, Horton, Athey, Dell, Gentzkow foundational papers
- New resources: Stanford GSB ML course, AEA webcasts, prompt engineering guides, Korinek newsletter
- New conferences: NBER Economics of AI, MLESI, SoFiE, ACM EC

## 2026-01-02
- Integrated model_score into search as post-RRF boost (0.4 weight)
- Added popularity boost toggle in search modal (📈 icon, default ON)

## 2026-01-01
- Added viewability signal to ranking model (hybrid: clicks×5 + impressions×0.5 + viewable×0.1 + dwell×1)
- Surfaces content users actually viewed, not just loaded

## 2025-12-31
- Added per-interaction AUC metrics to ranking evaluation
- Migrated analytics to D1 database with ML-ready schema

## 2025-12-30
- Added model_score field to content items for ranking
- Implemented category-level rankings

## 2025-12-29
- Upgraded to bge-large-en-v1.5 embeddings (1024 dims)
- Added weighted shuffle for Discover tab
