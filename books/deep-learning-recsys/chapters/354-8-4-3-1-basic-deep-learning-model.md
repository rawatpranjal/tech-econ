## 8.4.3.1 Basic Deep Learning Model

Based on the classical Embedding+MLP deep learning model architecture, the embedding of user behavior history is simply added and pooled, then connected with other user features, ad features, and scene features before being input to the upper neural network for training. The model structure is shown in Figure 8.13(a).

